import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root")); ///how we render what we want to this location.
//webpack connects react components to index.html
//webpack dependecy automatically makes the connection to the index.html
